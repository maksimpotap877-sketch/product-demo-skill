import {createServer} from 'node:http';
import {readFile,stat} from 'node:fs/promises';
import path from 'node:path';
import {createReadStream} from 'node:fs';
import {readJson,safeFile} from './util.js';
export async function previewRun(runDir:string,port=0,profile?:string){let plan=await readJson(path.join(runDir,profile?`edit-plan-${profile}.json`:'edit-plan.json'));try{await readJson(path.join(runDir,`renders/render-${plan.profile.name}.json`));}catch{plan=await readJson(path.join(runDir,'edit-plan-web-60.json'));}const render=await readJson(path.join(runDir,`renders/render-${plan.profile.name}.json`));const quality=await readJson(path.join(runDir,'review/quality-report.json'));const story=await readJson(path.join(runDir,'storyboard.json'));const narration=await readJson(path.join(runDir,'narration/narration-manifest.json'));const files=new Map<string,string>([['video',path.relative(runDir,render.file)],['contact','review/contact-sheet.png'],['report','review/quality-report.json']]);narration.segments.forEach((s:any,i:number)=>files.set('audio'+i,s.path));const esc=(v:any)=>String(v).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]!));const html=`<!doctype html><html lang="ru"><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Product Demo Review</title><style>body{max-width:1100px;margin:44px auto;background:#111827;color:#f3f4f6;font:18px Arial}video,img{width:100%;border-radius:16px}article{padding:20px;background:#1f2937;border-radius:16px;margin:18px 0}small{color:#aab6c9}</style><h1>${esc(story.title)}</h1><p>Статус: ${esc(quality.status)} · ${plan.profile.width}×${plan.profile.height} · render ${plan.profile.fps} fps · hybrid</p><video controls preload="metadata" src="/media/video"></video><p><small>Синтетический голос. Native capture 144 fps не подтверждён. Исходники и результат: ${esc(runDir)}</small></p>${story.scenes.map((s:any,i:number)=>`<article><h2>${esc(s.displayText)}</h2><p>${esc(s.spokenText)}</p><audio controls src="/media/audio${i}"></audio></article>`).join('')}<h2>Проверка качества</h2>${quality.checks.map((c:any)=>`<p>${esc(c.status)} — ${esc(c.name)}</p>`).join('')}<img src="/media/contact"><p><a href="/media/report">Quality report JSON</a></p>`;
 const server=createServer(async(req,res)=>{try{const url=new URL(req.url??'/','http://127.0.0.1');if(url.pathname==='/'){res.writeHead(200,{'Content-Type':'text/html; charset=utf-8','Content-Security-Policy':"default-src 'self'; style-src 'unsafe-inline'; media-src 'self'; img-src 'self'"});res.end(html);return;}const key=url.pathname.slice('/media/'.length);if(!url.pathname.startsWith('/media/')||!files.has(key)){res.writeHead(404);res.end();return;}const file=await safeFile(runDir,files.get(key)!);const {size}=await stat(file);const ext=path.extname(file);const mime=ext==='.mp4'?'video/mp4':ext==='.wav'?'audio/wav':ext==='.mp3'?'audio/mpeg':ext==='.png'?'image/png':'application/json';const range=req.headers.range?.match(/^bytes=(\d+)-(\d*)$/);if(range){const start=Number(range[1]),end=Math.min(size-1,range[2]?Number(range[2]):size-1);if(start>end||start>=size){res.writeHead(416);res.end();return;}res.writeHead(206,{'Content-Type':mime,'Content-Range':`bytes ${start}-${end}/${size}`,'Content-Length':end-start+1,'Accept-Ranges':'bytes'});createReadStream(file,{start,end}).pipe(res);}else{res.writeHead(200,{'Content-Type':mime,'Content-Length':size,'Accept-Ranges':'bytes'});createReadStream(file).pipe(res);}}catch{res.writeHead(404);res.end();}});await new Promise<void>(r=>server.listen(port,'127.0.0.1',r));const address=server.address() as any;return {url:`http://127.0.0.1:${address.port}`,server};}
